# Local Docker services (Postgres + Redis). Copy to puls3_server/.env, or run
# scripts/setup-local-secrets.sh to generate random values.
# The same values must be in config/passwords.yaml (database / redis).
POSTGRES_PASSWORD=
REDIS_PASSWORD=
POSTGRES_TEST_PASSWORD=
REDIS_TEST_PASSWORD=
