name: puls3_domain
description: Pure Dart domain model of puls3 (entities, value objects, ports and domain errors). No framework or chain dependencies.
publish_to: none

environment:
  sdk: '^3.8.0'

resolution: workspace

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  test: ^1.25.5
