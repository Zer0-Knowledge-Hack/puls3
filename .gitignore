# Local secrets. Generate them with scripts/setup-local-secrets.sh.
# Do not commit them (public repo). Tracked in #30 (testnet configuration and secrets management).
puls3_server/.env
.vscode/launch.json
.github/workflows/tests.yml

# Files and directories created by pub
.dart_tool/
.packages

# Pubspec overrides
pubspec_overrides.yaml

.vscode/settings.json

# Sensitive / never-commit files
**/.env
**/.env.*
!**/.env.example
*.pem
*.key
*.p12
*.secret
secrets/
.stellar/
.soroban/
.config/stellar/
*.log
.DS_Store
